# {{ .ProjectName }}

🚀 A Go backend service built with Gin, generated using Repo Beaver.

---

## 📦 Tech Stack

- Go (Golang)
- [Gin](https://github.com/gin-gonic/gin)

---

## 📁 Project Structure

```text
{{ .ProjectName }}/
├── cmd/
│   └── {{ .ProjectName }}/
│       └── main.go
├── internal/
│   ├── handlers/
│   ├── services/
│   ├── repositories/
│   ├── models/
│   ├── domains/
│   └── core/
├── pkg/
│   ├── logger/
│   └── utils/
├── configs/
├── go.mod
└── README.md
```

---

## 🚀 Getting Started

### 1. Clone the repository:
   ```bash
   git clone
   ```

### 2. Navigate to the project directory:
   ```bash
   cd {{ .ProjectName }}
   ```

### 3. Install dependencies:
   ```bash
   go mod tidy
   ```

### 4. Run the application:
   ```bash
   go run cmd/{{ .ProjectName }}/main.go
   ```

---

## 🌐 API

### Health Check

```
GET /health
```

### Response:

```json
{ "status": "ok", "service": "{{ .ProjectName }}" }
```

### Root

```
GET /
```

### Response:

```json
{ "message": "🚀 {{ .ProjectName }} running with Gin!" }
```

---

## 📌 Notes

- Default port: 8080
- Override the port using the `PORT` environment variable
- cmd/ → entry point of the application
- internal/ → private application logic
- pkg/ → reusable/shared code

---

## ✨ Generated by Repo Beaver
Happy coding 🦫
